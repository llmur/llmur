[project]
name = "llmur-integration-tests"
version = "0.1.0"
description = "Integration tests for the LLMur API"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31.0",
  "pytest>=7.4.0",
  "pytest-html>=3.2.0",
  "python-dotenv>=1.0.0",
  "openai",
]
